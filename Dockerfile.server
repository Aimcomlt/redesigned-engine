# Simple Node API image
FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY tsconfig.json ./
COPY server ./server
COPY src ./src

EXPOSE 3001
CMD ["npm", "run", "server"]
